public class Game;
	private level levelOne;
	private level levelTwo;	
	private level levelThree;

public class Level;
public Boolean goalReached()
	if Level = goalReached = true

	else goalReached = false
			
public int getPoints()
public int getScore()

if levelOne = goalReached
	return getScore;
	if levelOne + levelTwo = goalReached()
		return getScore;
		if levelOne + levelTwo + levelThree = goalReached()
			return getScore;
else
	return getScore null;

getScore = (levelOne + levelTwo + levelThree)

public Boolean isBonus();
	if isBonus = true 
	int (getScore * 3)


public class playManyTimes;
	while playManyGames 
